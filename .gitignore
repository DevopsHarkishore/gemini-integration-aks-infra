# Local .terraform directories
**/.terraform/*

# .tfstate files
*.tfstate
*.tfstate.*

# Crash log files
crash.log
crash.*.log

# Exclude sensitive vars
#.tfvars
# Except for terraform.tfvars which is often used for defaults, 
# but in this case it contains specific values we might want to keep or exclude.
# Usually, we exclude it if it contains secrets.
# I'll keep it for now as it's a dev setup, but exclude any .env or secret files.

# OS files
.DS_Store
Thumbs.db
